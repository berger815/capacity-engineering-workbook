import { useEffect, useMemo, useState } from "react";
import type {
  ApplicabilityState,
  CapacityModel,
  CapacityUnit,
  PhaseAllocation,
  ResourceKind,
  RoutingOperation,
  RoutingRevision,
} from "@capacity/domain";

export type ModelMutator = (entity: string, change: (next: CapacityModel) => void) => void;

interface EditorProps {
  model: CapacityModel;
  mutate: ModelMutator;
  targetId?: string;
  parentTargetId?: string;
}

function newId(prefix: string): string {
  return `${prefix}-${crypto.randomUUID()}`;
}

function optionalText(target: Record<string, unknown>, key: string, value: string): void {
  if (value.trim()) target[key] = value.trim();
  else delete target[key];
}

export function ProductsEditor({ model, mutate, targetId }: EditorProps) {
  const references = useMemo(() => new Set([
    ...model.demand.map(item => item.productId),
    ...model.routingRevisions.map(item => item.productId),
    ...(model.footprintPlans ?? []).flatMap(item => item.productId ? [item.productId] : []),
    ...(model.planningWip ?? []).map(item => item.productId),
  ]), [model]);

  function addProduct(): void {
    const organizationNodeId = model.organization[0]?.id;
    if (!organizationNodeId) return;
    mutate("products", next => next.products.push({ id: newId("product"), name: "New product", organizationNodeId }));
  }

  return <div className="table-card"><div className="card-title-row"><div><h3>Products</h3><small>Canonical product master used by routing, demand, footprint, and WIP.</small></div><button className="secondary" type="button" onClick={addProduct}>Add product</button></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Name</th><th>Family</th><th>Organization</th><th /></tr></thead><tbody>{model.products.map(product => <tr key={product.id} className={targetId === product.id ? "selected-row" : ""}><td><code>{product.id}</code></td><td><input value={product.name} onChange={event => mutate("products", next => { const found = next.products.find(item => item.id === product.id); if (found) found.name = event.target.value; })} /></td><td><input value={product.family ?? ""} onChange={event => mutate("products", next => { const found = next.products.find(item => item.id === product.id); if (found) optionalText(found as unknown as Record<string, unknown>, "family", event.target.value); })} /></td><td><select value={product.organizationNodeId} onChange={event => mutate("products", next => { const found = next.products.find(item => item.id === product.id); if (found) found.organizationNodeId = event.target.value; })}>{model.organization.map(node => <option key={node.id} value={node.id}>{node.name}</option>)}</select></td><td>{references.has(product.id) ? <span className="reference-lock">Referenced</span> : <button className="text-danger" type="button" onClick={() => mutate("products", next => { next.products = next.products.filter(item => item.id !== product.id); })}>Remove</button>}</td></tr>)}</tbody></table></div></div>;
}

export function CalendarsEditor({ model, mutate, targetId }: EditorProps) {
  const references = useMemo(() => new Set(model.resourceGroups.map(item => item.calendarId)), [model]);
  function addCalendar(): void {
    mutate("calendars", next => next.calendars.push({ id: newId("calendar"), name: "New calendar", timezone: "America/New_York", weeklyMinutes: { 1: 480, 2: 480, 3: 480, 4: 480, 5: 480 }, exceptions: [] }));
  }
  return <><div className="table-card"><div className="card-title-row"><div><h3>Working calendars</h3><small>Minutes are available by weekday before dated exceptions.</small></div><button className="secondary" type="button" onClick={addCalendar}>Add calendar</button></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Name</th><th>Timezone</th>{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(day => <th className="number" key={day}>{day}</th>)}<th /></tr></thead><tbody>{model.calendars.map(calendar => <tr key={calendar.id} className={targetId === calendar.id ? "selected-row" : ""}><td><code>{calendar.id}</code></td><td><input value={calendar.name} onChange={event => mutate("calendars", next => { const found = next.calendars.find(item => item.id === calendar.id); if (found) found.name = event.target.value; })} /></td><td><input value={calendar.timezone} onChange={event => mutate("calendars", next => { const found = next.calendars.find(item => item.id === calendar.id); if (found) found.timezone = event.target.value; })} /></td>{([0,1,2,3,4,5,6] as const).map(day => <td key={day}><input className="number-input" type="number" min="0" max="1440" value={calendar.weeklyMinutes[day] ?? 0} onChange={event => mutate("calendars", next => { const found = next.calendars.find(item => item.id === calendar.id); if (!found) return; const value = Number(event.target.value); if (value > 0) found.weeklyMinutes[day] = value; else delete found.weeklyMinutes[day]; })} /></td>)}<td>{references.has(calendar.id) ? <span className="reference-lock">Referenced</span> : <button className="text-danger" type="button" onClick={() => mutate("calendars", next => { next.calendars = next.calendars.filter(item => item.id !== calendar.id); })}>Remove</button>}</td></tr>)}</tbody></table></div></div>
    <div className="table-card"><div className="card-title-row"><div><h3>Calendar exceptions</h3><small>Shutdowns and temporary availability overrides.</small></div><button className="secondary" type="button" disabled={!model.calendars[0]} onClick={() => { const calendarId = targetId && model.calendars.some(item => item.id === targetId) ? targetId : model.calendars[0]?.id; if (!calendarId) return; mutate("calendars", next => next.calendars.find(item => item.id === calendarId)?.exceptions.push({ date: next.horizonStart, availableMinutes: 0, reason: "New exception" })); }}>Add exception</button></div><div className="table-wrap"><table><thead><tr><th>Calendar</th><th>Date</th><th className="number">Available minutes</th><th>Reason</th><th /></tr></thead><tbody>{model.calendars.flatMap(calendar => calendar.exceptions.map((exception, index) => <tr key={`${calendar.id}-${exception.date}-${index}`}><td>{calendar.name}</td><td><input type="date" value={exception.date} onChange={event => mutate("calendars", next => { const target = next.calendars.find(item => item.id === calendar.id)?.exceptions[index]; if (target) target.date = event.target.value; })} /></td><td><input className="number-input" type="number" min="0" value={exception.availableMinutes} onChange={event => mutate("calendars", next => { const target = next.calendars.find(item => item.id === calendar.id)?.exceptions[index]; if (target) target.availableMinutes = Number(event.target.value); })} /></td><td><input value={exception.reason ?? ""} onChange={event => mutate("calendars", next => { const target = next.calendars.find(item => item.id === calendar.id)?.exceptions[index]; if (target) optionalText(target as unknown as Record<string, unknown>, "reason", event.target.value); })} /></td><td><button className="text-danger" type="button" onClick={() => mutate("calendars", next => { const found = next.calendars.find(item => item.id === calendar.id); if (found) found.exceptions.splice(index, 1); })}>Remove</button></td></tr>))}</tbody></table></div></div></>;
}

export function ResourceGroupsEditor({ model, mutate, targetId }: EditorProps) {
  const references = useMemo(() => new Set([
    ...model.resources.map(item => item.resourceGroupId),
    ...model.routingRevisions.flatMap(item => item.operations.flatMap(operation => operation.requirements.map(requirement => requirement.resourceGroupId))),
    ...(model.scenarioActions ?? []).flatMap(action => action.kind === "resourceCapacityMultiplier" ? [action.resourceGroupId] : []),
  ]), [model]);
  function addGroup(): void {
    const organizationNodeId = model.organization[0]?.id;
    const calendarId = model.calendars[0]?.id;
    if (!organizationNodeId || !calendarId) return;
    mutate("resource-groups", next => next.resourceGroups.push({ id: newId("group"), name: "New resource group", organizationNodeId, kind: "labor", capacityUnit: "hours", calendarId, pooled: true }));
  }
  return <div className="table-card"><div className="card-title-row"><div><h3>Resource groups</h3><small>Constraint class, unit, calendar, and pooling basis.</small></div><button className="secondary" type="button" onClick={addGroup} disabled={!model.calendars.length}>Add group</button></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Name</th><th>Kind</th><th>Unit</th><th>Calendar</th><th>Organization</th><th>Pooled</th><th /></tr></thead><tbody>{model.resourceGroups.map(group => <tr key={group.id} className={targetId === group.id ? "selected-row" : ""}><td><code>{group.id}</code></td><td><input value={group.name} onChange={event => mutate("resource-groups", next => { const found = next.resourceGroups.find(item => item.id === group.id); if (found) found.name = event.target.value; })} /></td><td><select value={group.kind} onChange={event => mutate("resource-groups", next => { const found = next.resourceGroups.find(item => item.id === group.id); if (found) found.kind = event.target.value as ResourceKind; })}>{["labor","equipment","skill","tooling","space","external","other"].map(value => <option key={value} value={value}>{value}</option>)}</select></td><td><select value={group.capacityUnit} onChange={event => mutate("resource-groups", next => { const found = next.resourceGroups.find(item => item.id === group.id); if (found) found.capacityUnit = event.target.value as CapacityUnit; })}>{["hours","units","squareFeet","palletPositions","custom"].map(value => <option key={value} value={value}>{value}</option>)}</select></td><td><select value={group.calendarId} onChange={event => mutate("resource-groups", next => { const found = next.resourceGroups.find(item => item.id === group.id); if (found) found.calendarId = event.target.value; })}>{model.calendars.map(calendar => <option key={calendar.id} value={calendar.id}>{calendar.name}</option>)}</select></td><td><select value={group.organizationNodeId} onChange={event => mutate("resource-groups", next => { const found = next.resourceGroups.find(item => item.id === group.id); if (found) found.organizationNodeId = event.target.value; })}>{model.organization.map(node => <option key={node.id} value={node.id}>{node.name}</option>)}</select></td><td><input type="checkbox" checked={group.pooled} onChange={event => mutate("resource-groups", next => { const found = next.resourceGroups.find(item => item.id === group.id); if (found) found.pooled = event.target.checked; })} /></td><td>{references.has(group.id) ? <span className="reference-lock">Referenced</span> : <button className="text-danger" type="button" onClick={() => mutate("resource-groups", next => { next.resourceGroups = next.resourceGroups.filter(item => item.id !== group.id); })}>Remove</button>}</td></tr>)}</tbody></table></div></div>;
}

export function ResourcesEditor({ model, mutate, targetId }: EditorProps) {
  const references = useMemo(() => new Set((model.scenarioActions ?? []).flatMap(action => action.kind === "resourceQuantityDelta" ? [action.resourceId] : [])), [model]);
  function addResource(): void {
    const resourceGroupId = model.resourceGroups[0]?.id;
    if (!resourceGroupId) return;
    mutate("resources", next => next.resources.push({ id: newId("resource"), resourceGroupId, name: "New resource", quantity: 1, ratePerAvailableHour: 1, availability: 1, performance: 1, quality: 1 }));
  }
  return <div className="table-card"><div className="card-title-row"><div><h3>Resources</h3><small>Effective quantity, conversion rate, and availability factors.</small></div><button className="secondary" type="button" onClick={addResource} disabled={!model.resourceGroups.length}>Add resource</button></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Name</th><th>Group</th><th className="number">Qty.</th><th className="number">Rate</th><th className="number">Avail.</th><th className="number">Perf.</th><th className="number">Quality</th><th>From</th><th>To</th><th /></tr></thead><tbody>{model.resources.map(resource => <tr key={resource.id} className={targetId === resource.id ? "selected-row" : ""}><td><code>{resource.id}</code></td><td><input value={resource.name} onChange={event => mutate("resources", next => { const found = next.resources.find(item => item.id === resource.id); if (found) found.name = event.target.value; })} /></td><td><select value={resource.resourceGroupId} onChange={event => mutate("resources", next => { const found = next.resources.find(item => item.id === resource.id); if (found) found.resourceGroupId = event.target.value; })}>{model.resourceGroups.map(group => <option key={group.id} value={group.id}>{group.name}</option>)}</select></td>{(["quantity","ratePerAvailableHour","availability","performance","quality"] as const).map(field => <td key={field}><input className="number-input" type="number" min="0" step="0.01" value={resource[field]} onChange={event => mutate("resources", next => { const found = next.resources.find(item => item.id === resource.id); if (found) found[field] = Number(event.target.value); })} /></td>)}<td><input type="date" value={resource.effectiveFrom ?? ""} onChange={event => mutate("resources", next => { const found = next.resources.find(item => item.id === resource.id); if (found) optionalText(found as unknown as Record<string, unknown>, "effectiveFrom", event.target.value); })} /></td><td><input type="date" value={resource.effectiveTo ?? ""} onChange={event => mutate("resources", next => { const found = next.resources.find(item => item.id === resource.id); if (found) optionalText(found as unknown as Record<string, unknown>, "effectiveTo", event.target.value); })} /></td><td>{references.has(resource.id) ? <span className="reference-lock">Referenced</span> : <button className="text-danger" type="button" onClick={() => mutate("resources", next => { next.resources = next.resources.filter(item => item.id !== resource.id); })}>Remove</button>}</td></tr>)}</tbody></table></div></div>;
}

export function RoutingEditor({ model, mutate, targetId, parentTargetId }: EditorProps) {
  const [selectedRevisionId, setSelectedRevisionId] = useState(parentTargetId ?? targetId ?? model.routingRevisions[0]?.id ?? "");
  const revision = model.routingRevisions.find(item => item.id === selectedRevisionId) ?? model.routingRevisions[0];
  const [selectedOperationId, setSelectedOperationId] = useState(targetId && targetId !== selectedRevisionId ? targetId : revision?.operations[0]?.id ?? "");
  const operation = revision?.operations.find(item => item.id === selectedOperationId) ?? revision?.operations[0];

  useEffect(() => {
    if (parentTargetId && model.routingRevisions.some(item => item.id === parentTargetId)) setSelectedRevisionId(parentTargetId);
    else if (targetId && model.routingRevisions.some(item => item.id === targetId)) setSelectedRevisionId(targetId);
  }, [targetId, parentTargetId, model.routingRevisions]);

  useEffect(() => {
    if (revision && revision.id !== selectedRevisionId) setSelectedRevisionId(revision.id);
    if (revision && !revision.operations.some(item => item.id === selectedOperationId)) setSelectedOperationId(revision.operations[0]?.id ?? "");
  }, [revision, selectedRevisionId, selectedOperationId]);

  function addRevision(): void {
    const productId = model.products[0]?.id;
    const group = model.resourceGroups[0];
    if (!productId || !group) return;
    const id = newId("routing");
    const phaseId = `${id}:phase-1`;
    const operationId = `${id}:operation-10`;
    mutate("routing", next => next.routingRevisions.push({ id, productId, revision: "A", effectiveFrom: next.horizonStart, phases: [{ id: phaseId, name: "Production", startWeeksBeforeShip: 4, endWeeksBeforeShip: 0, allocation: "spread" }], operations: [{ id: operationId, sequence: 10, name: "Operation 10", phaseId, requirements: [{ id: `${operationId}:${group.id}`, resourceGroupId: group.id, requirement: { state: "value", value: 1, unit: group.capacityUnit } }] }], sourceSystem: "Assessment Studio edit" }));
    setSelectedRevisionId(id);
    setSelectedOperationId(operationId);
  }

  function addPhase(target: RoutingRevision): void {
    const id = `${target.id}:phase-${target.phases.length + 1}`;
    mutate("routing", next => next.routingRevisions.find(item => item.id === target.id)?.phases.push({ id, name: `Phase ${target.phases.length + 1}`, startWeeksBeforeShip: 4, endWeeksBeforeShip: 0, allocation: "spread" }));
  }

  function addOperation(target: RoutingRevision): void {
    const phaseId = target.phases[0]?.id;
    if (!phaseId) return;
    const id = `${target.id}:operation-${(target.operations.length + 1) * 10}`;
    mutate("routing", next => next.routingRevisions.find(item => item.id === target.id)?.operations.push({ id, sequence: (target.operations.length + 1) * 10, name: `Operation ${(target.operations.length + 1) * 10}`, phaseId, requirements: [] }));
    setSelectedOperationId(id);
  }

  function addRequirement(targetRevision: RoutingRevision, targetOperation: RoutingOperation): void {
    const group = model.resourceGroups.find(item => !targetOperation.requirements.some(requirement => requirement.resourceGroupId === item.id));
    if (!group) return;
    mutate("routing", next => next.routingRevisions.find(item => item.id === targetRevision.id)?.operations.find(item => item.id === targetOperation.id)?.requirements.push({ id: `${targetOperation.id}:${group.id}`, resourceGroupId: group.id, requirement: { state: "value", value: 1, unit: group.capacityUnit } }));
  }

  return <div className="routing-builder workbench-routing"><div className="routing-list card"><div className="card-title-row"><h3>Revisions</h3><button className="secondary" type="button" onClick={addRevision} disabled={!model.products.length || !model.resourceGroups.length}>Add revision</button></div>{model.routingRevisions.map(item => <button key={item.id} type="button" className={revision?.id === item.id ? "active" : ""} onClick={() => { setSelectedRevisionId(item.id); setSelectedOperationId(item.operations[0]?.id ?? ""); }}><strong>{model.products.find(product => product.id === item.productId)?.name ?? item.productId}</strong><small>{item.revision} · {item.effectiveFrom}</small></button>)}</div>
    {revision ? <div className="routing-detail"><div className="table-card"><div className="card-title-row"><div><h3>Revision</h3><small><code>{revision.id}</code></small></div><button className="text-danger" type="button" onClick={() => mutate("routing", next => { next.routingRevisions = next.routingRevisions.filter(item => item.id !== revision.id); })}>Remove revision</button></div><div className="routing-meta-grid"><label>Product<select value={revision.productId} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id); if (found) found.productId = event.target.value; })}>{model.products.map(product => <option key={product.id} value={product.id}>{product.name}</option>)}</select></label><label>Revision<input value={revision.revision} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id); if (found) found.revision = event.target.value; })} /></label><label>Effective from<input type="date" value={revision.effectiveFrom} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id); if (found) found.effectiveFrom = event.target.value; })} /></label><label>Effective to<input type="date" value={revision.effectiveTo ?? ""} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id); if (found) optionalText(found as unknown as Record<string, unknown>, "effectiveTo", event.target.value); })} /></label></div></div>
      <div className="table-card"><div className="card-title-row"><div><h3>Lead-time phases</h3><small>Operations must reference one phase.</small></div><button className="secondary" type="button" onClick={() => addPhase(revision)}>Add phase</button></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Name</th><th className="number">Start</th><th className="number">End</th><th>Allocation</th><th /></tr></thead><tbody>{revision.phases.map(phase => <tr key={phase.id}><td><code>{phase.id.split(":").slice(-1)[0]}</code></td><td><input value={phase.name} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.phases.find(item => item.id === phase.id); if (found) found.name = event.target.value; })} /></td><td><input className="number-input" type="number" min="0" value={phase.startWeeksBeforeShip} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.phases.find(item => item.id === phase.id); if (found) found.startWeeksBeforeShip = Number(event.target.value); })} /></td><td><input className="number-input" type="number" min="0" value={phase.endWeeksBeforeShip} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.phases.find(item => item.id === phase.id); if (found) found.endWeeksBeforeShip = Number(event.target.value); })} /></td><td><select value={phase.allocation} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.phases.find(item => item.id === phase.id); if (found) found.allocation = event.target.value as PhaseAllocation; })}>{["spread","shiftToStart","shiftToEnd","shiftToMidpoint"].map(value => <option key={value} value={value}>{value}</option>)}</select></td><td>{revision.phases.length === 1 || revision.operations.some(item => item.phaseId === phase.id) ? <span className="reference-lock">Referenced</span> : <button className="text-danger" type="button" onClick={() => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id); if (found) found.phases = found.phases.filter(item => item.id !== phase.id); })}>Remove</button>}</td></tr>)}</tbody></table></div></div>
      <div className="routing-columns"><div className="table-card"><div className="card-title-row"><div><h3>Operations</h3><small>Select an operation to edit requirements.</small></div><button className="secondary" type="button" onClick={() => addOperation(revision)}>Add operation</button></div><div className="table-wrap"><table><thead><tr><th>Seq.</th><th>Name</th><th>Phase</th><th /></tr></thead><tbody>{revision.operations.map(item => <tr key={item.id} className={operation?.id === item.id ? "selected-row" : ""} onClick={() => setSelectedOperationId(item.id)}><td><input className="number-input" type="number" min="0" value={item.sequence} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(candidate => candidate.id === revision.id)?.operations.find(candidate => candidate.id === item.id); if (found) found.sequence = Number(event.target.value); })} /></td><td><input value={item.name} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(candidate => candidate.id === revision.id)?.operations.find(candidate => candidate.id === item.id); if (found) found.name = event.target.value; })} /></td><td><select value={item.phaseId} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(candidate => candidate.id === revision.id)?.operations.find(candidate => candidate.id === item.id); if (found) found.phaseId = event.target.value; })}>{revision.phases.map(phase => <option key={phase.id} value={phase.id}>{phase.name}</option>)}</select></td><td>{revision.operations.length === 1 ? <span className="reference-lock">Required</span> : <button className="text-danger" type="button" onClick={event => { event.stopPropagation(); mutate("routing", next => { const found = next.routingRevisions.find(candidate => candidate.id === revision.id); if (found) found.operations = found.operations.filter(candidate => candidate.id !== item.id); }); }}>Remove</button>}</td></tr>)}</tbody></table></div></div>
        {operation ? <div className="table-card"><div className="card-title-row"><div><h3>{operation.name} requirements</h3><small>Only applicable resources belong here.</small></div><button className="secondary" type="button" onClick={() => addRequirement(revision, operation)}>Add requirement</button></div><div className="table-wrap"><table><thead><tr><th>Resource group</th><th>State</th><th className="number">Value</th><th>Unit</th><th className="number">Setup</th><th className="number">Batch</th><th /></tr></thead><tbody>{operation.requirements.map(requirement => <tr key={requirement.id}><td><select value={requirement.resourceGroupId} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.operations.find(item => item.id === operation.id)?.requirements.find(item => item.id === requirement.id); if (!found) return; found.resourceGroupId = event.target.value; const group = next.resourceGroups.find(item => item.id === event.target.value); if (group) found.requirement.unit = group.capacityUnit; })}>{model.resourceGroups.map(group => <option key={group.id} value={group.id}>{group.name}</option>)}</select></td><td><select value={requirement.requirement.state} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.operations.find(item => item.id === operation.id)?.requirements.find(item => item.id === requirement.id); if (!found) return; found.requirement.state = event.target.value as ApplicabilityState; if (found.requirement.state !== "value") delete found.requirement.value; else found.requirement.value ??= 0; })}>{["value","missing","notApplicable","zero"].map(value => <option key={value} value={value}>{value}</option>)}</select></td><td><input className="number-input" type="number" min="0" step="0.001" disabled={requirement.requirement.state !== "value"} value={requirement.requirement.value ?? 0} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.operations.find(item => item.id === operation.id)?.requirements.find(item => item.id === requirement.id); if (found) found.requirement.value = Number(event.target.value); })} /></td><td>{requirement.requirement.unit}</td><td><input className="number-input" type="number" min="0" step="0.01" value={requirement.setupQuantity ?? 0} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.operations.find(item => item.id === operation.id)?.requirements.find(item => item.id === requirement.id); if (!found) return; const value = Number(event.target.value); if (value > 0) found.setupQuantity = value; else delete found.setupQuantity; })} /></td><td><input className="number-input" type="number" min="0" step="1" value={requirement.batchSize ?? 0} onChange={event => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.operations.find(item => item.id === operation.id)?.requirements.find(item => item.id === requirement.id); if (!found) return; const value = Number(event.target.value); if (value > 0) found.batchSize = value; else delete found.batchSize; })} /></td><td><button className="text-danger" type="button" onClick={() => mutate("routing", next => { const found = next.routingRevisions.find(item => item.id === revision.id)?.operations.find(item => item.id === operation.id); if (found) found.requirements = found.requirements.filter(item => item.id !== requirement.id); })}>Remove</button></td></tr>)}</tbody></table></div></div> : null}</div></div> : <div className="empty-state"><h3>No routing revisions</h3><p>Add products and resource groups, then create the first routing revision.</p><button className="primary" type="button" onClick={addRevision}>Add revision</button></div>}</div>;
}
